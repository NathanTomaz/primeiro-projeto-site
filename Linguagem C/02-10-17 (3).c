#include <stdio.h>

int main(void)
{
        int count, count2;
printf("Digite um numero: ");
scanf("%d", &count2);
        for(count=1 ; count2 >=count; count++)
        {
            printf("%d\n", count);
        }
}
